The Forgotten asset bundle
==========================

The game loads the original `tiny-RPG-forest-files` art and map bundle from
the project root. This distribution keeps the source and launcher portable;
place that bundle beside the Python files when using the original artwork.

The game logic includes a headless-safe Map collision interface and does not
store generated or machine-specific asset files in source control.
